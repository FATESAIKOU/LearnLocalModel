# Step 010 — Aider 階段三：系統基礎環境安裝

## 背景

階段三開始執行實際安裝與環境建置。第一步先處理 Ubuntu 系統層的必要套件，確保後續能安裝 Aider 並配合 Git 使用。

## 本步目標

| 項目 | 內容 |
|---|---|
| 安裝套件 | `python3`, `python3-pip`, `git` |
| 驗證方式 | 逐一檢查版本 |

## command.md 內容

本步將把 `command.md` 改為只包含本步需要的 Ubuntu 指令，避免與其他步驟混淆。

## 執行結果

| 項目 | 結果 |
|---|---|
| `apt update` | 成功 |
| `python3` | 已存在，版本 `3.12.7` |
| `python3-pip` | 已存在，版本 `24.2` |
| `git` | 已存在，版本 `2.43.0` |

## 結論

系統基礎環境已完成，可進入下一步安裝 `aider-chat`。
